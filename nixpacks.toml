[phases.setup]
nixPkgs = ["python312Full"]

[phases.install]
cmds = [
    "python -m ensurepip --upgrade",
    "python -m pip install -r requirements.txt",
    "python manage.py collectstatic --noinput",
]

[start]
cmd = "gunicorn hotel_management.wsgi --bind 0.0.0.0:$PORT --workers 3 --log-file -"