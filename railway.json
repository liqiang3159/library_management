{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "gunicorn hotel_management.wsgi --bind 0.0.0.0:$PORT --workers 3 --log-file -",
    "restartPolicyType": "ON_FAILURE"
  }
}
